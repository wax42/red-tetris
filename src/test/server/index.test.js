import { handleClient } from "../../server/index";
import { JestEnvironment } from "@jest/environment";
describe(" SERVER/INDEX.JS", () => {
  /* const cb = jest.fn();
  const onMock = (eventsocket, callbackClient) => {
    cb();
  };

  const client = {
    on: onMock
  };
  it("", () => {
    handleClient(client);
    //expect(client.on).toHaveBeenCalled();
    expect(cb).toHaveBeenCalled();
  }); */
  it("" => {
    
  })
});
