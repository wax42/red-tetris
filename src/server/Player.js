class Player {
  constructor(name, clientSocket) {
    this.score = 0;
    this.name = name;
    this.id = "yo";

    this.grid = [];
  }
  generateSpectrum() {
    // Take the grid and return a spectrum
  }
}

module.exports = Player;
